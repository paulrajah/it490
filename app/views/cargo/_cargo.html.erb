<%= hidden_field_tag :direction, params[:direction] %>
<%= hidden_field_tag :sort, params[:sort] %>

<%= will_paginate @cargo %>

<table width="900">
	<thead>
		<tr>
			<th><%= sortable "id", "Cargo Id" %></th>
			<th><%= sortable "owner" %></th>
			<th><%= sortable "contents" %></th>
			<th><%= sortable "weight" %></th>
			<th>Flight #</th>
			<th>Origin</th>
			<th>Destination</th>
			<th>Departure</th>
			<th>Arrival</th>
		</tr>
	</thead>

	<tbody>
		<% @cargo.each do |c| %>
		<tr>
			<td><%= c.id %></td>
			<td><%= c.flight.user.email %></td>
			<td><%= c.contents %></td>
			<td><%= c.weight %></td>
			<td><%= c.flight.number %></td>
			<td><%= c.flight.origin.pretty_name %></td>
			<td><%= c.flight.destination.pretty_name %></td>
			<td><%= c.flight.departure_time %></td>
			<td><%= c.flight.arrival_time %></td>
		</tr>
		<% end %></tbody>
</table>

<%= will_paginate @cargo %>

<br>