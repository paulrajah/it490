<%= form_for(@airport) do |f| %>
  <% if @airport.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@airport.errors.count, "error") %> prohibited this airport from being saved:</h2>

      <ul>
      <% @airport.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :code %><br>
    <%= f.text_field :code, :maxlength => 3 %>
  </div>
  <div class="field">
    <%= f.label :name %><br>
    <%= f.text_field :name, :size => 35 %>
  </div>
  <div class="actions">
    <%= f.submit :class => 'button big gh-black'%>
  </div>
<% end %>
