<%= form_for(@aircraft_type) do |f| %>
  <% if @aircraft_type.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@aircraft_type.errors.count, "error") %> prohibited this aircraft type from being saved:</h2>

      <ul>
      <% @aircraft_type.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :code %><br>
    <%= f.text_field :code, :maxlength => 3 %>
  </div>
  <div class="field">
    <%= f.label :name %><br>
    <%= f.text_field :name, :size => 35 %>
  </div>
  <div class="actions">
    <%= f.submit :class => 'button big gh-black'%>
  </div>
<% end %>
